<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:NextHolliday"
             x:Class="NextHolliday.MainPage">

  <StackLayout HorizontalOptions="Center" VerticalOptions="Center" Orientation="Horizontal">

    <Label Text="{Binding RemainingTime.Days}" FontSize="Large" />
     <Label Text="{Binding RemainingTime.Hours}" FontSize="Large" />
    <Label Text="{Binding RemainingTime.Minutes}" FontSize="Large" />
    <Label Text="{Binding RemainingTime.Seconds}" FontSize="Large" />
  
  </StackLayout>

</ContentPage>