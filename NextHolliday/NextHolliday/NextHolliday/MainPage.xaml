<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:NextHolliday"
             x:Class="NextHolliday.MainPage">

  <StackLayout>

    <Label Text="{Binding RemainingTime.Days}" />
    <Label Text="{Binding RemainingTime.Hours}" />
    <Label Text="{Binding RemainingTime.Minutes}" />
    <Label Text="{Binding RemainingTime.Seconds}" />
  
  </StackLayout>

</ContentPage>